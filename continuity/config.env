# ~/.openclaw/skills/continuity/config.env
# Continuity Skill Configuration
# Version: 0.2.0
#
# This file configures the behavior of the continuity skill.
# All settings have sensible defaults - only uncomment and change what you need.
# SAFETY: This file contains no secrets. Keep it at 600 permissions.

# ============================================
# RECALL MODE: How to retrieve previous context
# ============================================
# Options:
#   off              - Don't recall previous context automatically
#   openclaw_only    - Only use OpenClaw's native memory system
#   continuity_only  - Only use continuity action stream logs
#   both             - Combine both sources (default, recommended)
# ============================================
#CONTINUITY_RECALL_MODE=both

# ============================================
# LOG LEVEL: What to automatically log
# ============================================
# Options:
#   off        - Disable automatic logging (manual only)
#   judgment   - Only log "judgment" responses (decisions, analysis)
#   everything - Log all responses (default, recommended)
# ============================================
#CONTINUITY_LOG_LEVEL=everything

# ============================================
# ADVANCED SETTINGS (rarely need changing)
# ============================================

# Maximum entries to recall in auto-recall mode
#CONTINUITY_RECALL_LIMIT=10

# Enable strict integrity validation on load (slower but safer)
#CONTINUITY_STRICT_VALIDATION=false

# Emergency mode - log to stderr if disk write fails
#CONTINUITY_EMERGENCY_STDERR=true

# Base directory for continuity data
#CONTINUITY_BASE_DIR=${HOME}/clawd/continuity
